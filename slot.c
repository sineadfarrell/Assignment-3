#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void)
{
	int numSlots[20];
	printf("Select a number of slots");
	scanf("%d", &numSlots);
	printf("%d", numSlots);
	
	
	return 0;
}